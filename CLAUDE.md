# personal-workstation

个人工作台。第一期是**四级精听工作台**：听写驱动的英语训练台，训练行为本身产出全部数据（零手工录入），错词自动进 FSRS 间隔重复队列。四级是第一个里程碑，六级复用同一套系统。

本文件是项目规则，个人通用偏好见 `~/.claude/CLAUDE.md`。

---

## 1. 先查知识库，再动代码

**回答任何功能/机制/设计问题之前，先读 `docs/kb/`**，不要一上来就翻代码。知识库记的是"为什么"，代码只有"是什么"。

| 问题类型 | 先看 |
|---|---|
| **改任何东西之前（必读）** | [docs/kb/user-profile.md](docs/kb/user-profile.md) —— 使用者画像与硬约束。这个项目看起来像"四级备考 App"，实际上不是，丢掉这页必然走偏 |
| **修 bug（必读）** | [docs/kb/pitfalls.md](docs/kb/pitfalls.md) —— 很多"新 bug"是老坑换了个表现 |
| 为什么这么做 / 为什么不那么做 | [docs/kb/decisions.md](docs/kb/decisions.md) |
| 四档强度、关键词口径、diff 规则、挖空规则 | [docs/kb/dictation-engine.md](docs/kb/dictation-engine.md) |
| 错词调度、评分映射、毕业条件 | [docs/kb/srs.md](docs/kb/srs.md) |
| 素材来源、版权红线、切句降级 | [docs/kb/materials.md](docs/kb/materials.md) |
| 表结构、字段为什么这么设计 | [docs/kb/data-model.md](docs/kb/data-model.md) |

知识库没覆盖 → 再去读代码。**读出来的结论要补回知识库**，别让下一个人重读一遍。

### 设计快照 vs 活文档

- `docs/superpowers/specs/` —— 设计**快照**，定格在成文那天，**不要改**。想知道"当初为什么这么定"时看它
- `docs/kb/` —— **活文档**，随实现演进。想知道"现在是怎么回事"时看它

两者冲突时**以 kb 为准**，并在 kb 里说明与原设计的偏离及原因。

## 2. 改完必须维护知识库

这是硬要求，和写代码同等重要：

- **改听写判定逻辑**（档位、关键词口径、归一化、容错、计分）→ 更新 `dictation-engine.md`。这块规则互相牵制，改一处不同步文档，下一个人必然做出矛盾的实现
- **改调度逻辑**（rating 映射、联动、毕业条件）→ 更新 `srs.md`
- **改表结构** → 更新 `data-model.md`，并写清**为什么**这么改（那张"刻意的设计"表就是防止有人"顺手优化"掉它们）
- **修 bug** → 在 `pitfalls.md` 加一条：现象、根因、修法、回归测试在哪。这是这个目录里最值钱的文件
- **加了新的决策或否决了某个方案** → 补 `decisions.md`，**连同理由**。否则半年后会有人重新提一遍，再论证一遍
- 只写非显然的东西。字段名、函数签名这类看代码就知道的别抄进去，抄一遍就多一处会过期的地方

## 3. 版权红线

> **本仓库不包含、不分发任何听力素材。**

`.gitignore` 已排除 `materials/`、`*.mp3`、`*.wav`、`*.m4a`、`*.lrc`、`*.srt`。**仓库是公开的**，任何时候都不要把音频或真题文本提交进去。提交前扫一眼 `git status`。

## 4. 范围纪律

第一期**只做四级听力这一条线**。以下一律不做，有人提就回去看 [decisions.md](docs/kb/decisions.md)：

- 内置词表背单词（使用者词汇量已覆盖四级，无效劳动）
- 阅读 / 写作 / 翻译模块（使用者不缺）
- 优先级调度、每日任务推荐、时间块规划（使用者明确**没选**"抢时间"这个痛点）
- 手机端 / 飞书推送
- 其余四条线（读研 / 上班 / 辅导孩子 / 养生）—— 数据模型留了 `lane` 扩展位，但第一期不要碰

## 5. 验证标准

| 改动类型 | 要跑什么 |
|---|---|
| **判定逻辑**（diff、计分、关键词、挖空、rating 映射） | 必须有单元测试，且覆盖 `pitfalls.md` 里列出的每个反例（`their/there`、`it's/its`、保命档 NULL 等）。这块靠肉眼验不出来 |
| 其他逻辑改动 | `npm run tsc` + `npm test`，两个都过才算完 |
| 纯 UI 改动（样式、布局、文案） | **不跑测试**，直接列出改了哪几个文件、改了什么，我自己看界面 |
| 修 bug | 必须有回归测试，且**能证明自己有效**：把修复 stash 掉跑一遍，用例必须失败 |

## 6. 注释风格

只写**为什么**——踩过的坑、规则之间的牵制关系、刻意不那么写的理由。

- **不写**代码已经说清的事
- **不做**被删代码的考古
- 判定逻辑里凡是"看起来可以简化"的地方，都要留一句为什么不能简化（例如 `'s` 不展开、保命档 accuracy 为 NULL），否则一定会被后来者"顺手优化"掉

## 7. 提交

- **提交和推送前都要先问我**
- commit message **英文**（Conventional Commits）
- 原子提交：不同逻辑单元分开提
- 远程：https://github.com/longqiUTR/personal-workstation.git
