# 国内镜像。官方 registry 走代理约 11s/包，这里约 3.7s，装依赖时差距会放大到几十分钟。
registry=https://registry.npmmirror.com

# better-sqlite3 的预编译二进制默认从 GitHub Releases 拉，国内基本拉不动，
# 拉不到就会回落到本地 node-gyp 编译（需要 Visual Studio Build Tools）。
better_sqlite3_binary_host_mirror=https://registry.npmmirror.com/-/binary/better-sqlite3
